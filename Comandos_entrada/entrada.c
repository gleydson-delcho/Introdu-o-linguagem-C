// Diretiva de pré-compilação - uso de bibliotecas.

#include <stdio.h>

void main() {
    int numero;

    // printf() imprimi na tela o que está dentro da função
    printf("Digite um número inteiro:\n");

    // scanf() faz a leitura e armazena na variável aquilo que foi informado.
    scanf("%d", &numero);

    //printf("O valor informado foi numero = %d", numero);
}